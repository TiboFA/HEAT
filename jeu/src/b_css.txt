<style>:root{
  --plane:#0d0d0d; --surface:#1a1a19; --surface-2:#222221; --sea:#111110;
  --ink:#fff; --ink-2:#c3c2b7; --muted:#898781;
  --grid:#2c2c2a; --ring:rgba(255,255,255,.10);
  --s-traj:#3987e5; --s-reel:#199e70; --s-percu:#c98500; --s-fric:#d03b3b;
  --s-base:#9085e9; --s-autres:#d95926; --ok:#0ca30c;
  --land:#2f2f2c; --land-hi:#3f3f3a; --land-sel:#4b4b45; --land-lock:#3a2f2f;
}
*{box-sizing:border-box}
body{margin:0;background:var(--plane);color:var(--ink);
 font:14px/1.45 system-ui,-apple-system,"Segoe UI",sans-serif;-webkit-font-smoothing:antialiased}
h1,h2,h3,h4{margin:0;font-weight:600}
.wrap{max-width:1680px;margin:0 auto;padding:14px}
button{font:inherit}

/* ---------- accueil ---------- */
#home{max-width:820px;margin:6vh auto;padding:0 18px}
#home h1{font-size:44px;letter-spacing:.22em;font-weight:700}
#home .sub{color:var(--muted);margin:8px 0 26px;font-size:14px}
.hsec{border:1px solid var(--ring);border-radius:12px;background:var(--surface);padding:16px 18px;margin-bottom:12px}
.hsec .ct{font-size:10px;letter-spacing:.11em;text-transform:uppercase;color:var(--muted);font-weight:700;margin-bottom:10px}
.opts{display:grid;grid-template-columns:1fr 1fr;gap:10px}
.opt{border:1px solid var(--ring);border-radius:10px;background:var(--surface-2);padding:12px 14px;cursor:pointer;text-align:left}
.opt[aria-pressed="true"]{border-color:var(--ink);background:#2c2c29}
.opt b{display:block;font-size:14px;margin-bottom:3px}
.opt span{color:var(--muted);font-size:11.5px;line-height:1.4;display:block}
.opts.four{grid-template-columns:repeat(4,1fr)}
/* ---------- reprise de partie ---------- */
.rpz{border:1px solid rgba(25,158,112,.45);border-left:3px solid var(--s-reel);border-radius:12px;
 background:linear-gradient(180deg,rgba(25,158,112,.08),transparent 70%);padding:13px 16px;margin-bottom:12px}
.rpt{font-size:13px;color:var(--ink-2);margin-bottom:9px}
.rpt b{color:var(--ink)}
.rpz.vide{border-color:var(--ring);border-left-color:var(--ring);background:var(--surface)}
.rpz.vide .rpt{color:var(--muted);font-size:12px}
.pbtn{display:flex;gap:8px;flex-wrap:wrap;align-items:center}
.mini.go{background:var(--ink);color:#0b0b0b;border-color:var(--ink);font-weight:700}
#start{width:100%;margin-top:6px;border:0;border-radius:10px;padding:14px;font-weight:700;font-size:15px;
 background:var(--ink);color:#0b0b0b;cursor:pointer}
/* ---------- accueil : l'écran doit tenir en une page et donner envie ----------
   Tout ce qui n'est pas le pitch, le choix du camp et le bouton est replié dans
   un <details>. Rien n'a été supprimé : les règles complètes, la difficulté et
   la doctrine sont à un clic, et c'est très bien là. */
#home .pitch{margin:14px 0 26px;max-width:660px}
#home .pitch p{color:var(--ink-2);font-size:15px;line-height:1.65;margin:0 0 10px}
#home .pitch b{color:var(--ink)}
#home .pitch i{color:var(--ink);font-style:italic}
.hsec.camps{padding:18px 20px}
.camps .opt{padding:16px 18px}
.camps .opt b{font-size:17px;margin-bottom:6px}
.camps .opt span{font-size:13px;line-height:1.55;color:var(--ink-2)}
.camps .opt span i{display:block;margin-top:8px;color:var(--muted);font-style:normal;font-size:12px}
.startnote{color:var(--muted);font-size:12px;line-height:1.55;margin:10px 2px 22px;text-align:center}
.startnote b{color:var(--ink-2)}
.hdet{border:1px solid var(--ring);border-radius:12px;background:var(--surface);margin-bottom:10px;overflow:hidden}
.hdet>summary{cursor:pointer;list-style:none;padding:13px 18px;font-size:13px;font-weight:600;color:var(--ink-2);
  display:flex;align-items:center;gap:9px;user-select:none}
.hdet>summary::-webkit-details-marker{display:none}
.hdet>summary::before{content:"▸";color:var(--muted);font-size:11px;transition:transform .12s}
.hdet[open]>summary::before{transform:rotate(90deg)}
.hdet>summary:hover{color:var(--ink)}
.hdet[open]>summary{border-bottom:1px solid var(--ring);color:var(--ink)}
.hdet>.rules,.hdet>.ct,.hdet>.opts,.hdet>.doc{margin-left:18px;margin-right:18px}
.hdet>.rules:last-child{padding-bottom:16px}
.hdet>.doc{padding-bottom:16px}
.hdet>.opts{margin-bottom:4px}
.vers{color:var(--muted);font-size:11px;line-height:1.5;margin-top:22px;text-align:center}
.rpz.rien{border:0;background:none;padding:0;margin:-14px 0 22px;text-align:center;font-size:12px}
#home .rules{color:var(--ink-2);font-size:12.5px;line-height:1.6}
#home .rules b{color:var(--ink)}

/* ---------- appli ---------- */
.app{border:1px solid var(--ring);border-radius:14px;background:var(--surface);overflow:hidden}
.topbar{display:flex;align-items:center;gap:14px;padding:10px 14px;border-bottom:1px solid var(--ring);
 background:var(--surface-2);flex-wrap:wrap}
.brand{font-weight:700;letter-spacing:.16em;font-size:12.5px}
.turn{color:var(--ink-2);font-size:12.5px}.turn b{color:var(--ink);font-variant-numeric:tabular-nums}
.res{display:flex;gap:12px;margin-left:auto;flex-wrap:wrap}
.res div{display:flex;align-items:center;gap:6px;font-size:12px;color:var(--ink-2);cursor:help}
.res .v{color:var(--ink);font-weight:700;font-variant-numeric:tabular-nums}
.res .ecr{font-size:11px;font-weight:700;font-variant-numeric:tabular-nums;padding:1px 6px;border-radius:6px;
  border:1px solid var(--ring);color:var(--muted);white-space:nowrap}
.res .ecr.bon{border-color:rgba(25,158,112,.55);color:var(--s-reel)}
.res .ecr.mal{border-color:rgba(208,59,59,.5);color:var(--s-fric)}
.pill{width:8px;height:8px;border-radius:2px}
.endturn{border:0;border-radius:8px;padding:8px 16px;font-weight:700;cursor:pointer;background:var(--ink);color:#0b0b0b}
.endturn:disabled{opacity:.45;cursor:default}

.events{display:flex;gap:9px;padding:10px 14px;border-bottom:1px solid var(--ring);background:var(--surface-2);overflow-x:auto}
.ev{flex:1;min-width:260px;border:1px solid var(--ring);border-left:3px solid var(--s-percu);border-radius:9px;
 background:var(--surface);padding:8px 10px;display:flex;flex-direction:column;gap:5px}
.ev.done{border-left-color:var(--ok)}
.ev.cold{border-left-color:var(--muted)}
.ev .eh{display:flex;align-items:baseline;gap:8px}.ev .eh h4{font-size:12px}
.ev .eh .loc{color:var(--muted);font-size:10.5px;margin-left:auto;white-space:nowrap}
.ev p{margin:0;color:var(--ink-2);font-size:11px}
.mini{border:1px solid var(--ring);background:var(--surface-2);color:var(--ink);border-radius:7px;
 padding:4px 9px;font-size:11px;font-weight:600;cursor:pointer}
.mini:hover:not(:disabled){border-color:var(--ink-2)}
.mini:disabled{opacity:.4;cursor:default}
.st{color:var(--muted);font-size:10.5px}

/* ---------- bandeau « votre tour » ---------- */
.plan{background:var(--surface);border-bottom:1px solid var(--ring);padding:9px 13px}
.plan.vide{padding:7px 13px}
.plan.alerte{box-shadow:inset 3px 0 0 var(--s-fric)}
.pmsg{font-size:11px;color:var(--ink-2)}
.pmsg span{color:var(--muted)}
.phead{display:flex;align-items:baseline;gap:10px;margin-bottom:7px}
.pt{font-size:9.5px;letter-spacing:.11em;text-transform:uppercase;color:var(--muted);font-weight:700}
.pt span{letter-spacing:0;text-transform:none;font-weight:400;margin-left:8px;color:var(--ink-2)}
.pacts{margin-left:auto;font-size:10.5px}
.lnk{color:var(--ink-2);text-decoration:underline;cursor:pointer}
.lnk:hover{color:var(--ink)}
.lnk.off{opacity:.4;cursor:default;text-decoration:none}
.pchips{display:flex;gap:7px;flex-wrap:wrap}
.pchip{position:relative;border:1px solid var(--ring);border-left:3px solid var(--s-traj);border-radius:8px;
 background:var(--surface-2);padding:6px 26px 6px 9px;min-width:150px;cursor:help}
.pchip.attr{border-left-color:var(--s-percu)}
.pchip.ko{border-left-color:var(--s-fric);opacity:.75}
.pchip.ko .pn{text-decoration:line-through}
.pn{font-size:11.5px;font-weight:600;line-height:1.25}
.pn i{font-style:normal;color:var(--muted);font-weight:700;font-size:9.5px;margin-right:5px;letter-spacing:.04em}
.pmv em{font-style:normal;font-weight:700}
.pmv em.contr{color:var(--s-traj)}
.pmv em.reel{color:var(--s-reel)}
.pmv em.percu{color:var(--s-percu)}
.pmv em.fric{color:var(--s-fric)}
.pmv span{display:inline-flex;gap:7px;align-items:baseline}
.pd{font-size:9.5px;color:var(--muted);margin-top:2px}
.pd .pr{color:var(--s-percu);font-weight:700}
.pd b{color:var(--s-fric)}
.px{position:absolute;top:4px;right:4px;width:17px;height:17px;line-height:1;border:0;border-radius:5px;
 background:transparent;color:var(--muted);cursor:pointer;font-size:14px;padding:0}
.px:hover{background:var(--s-fric);color:#fff}
.pimp{display:flex;gap:1px;margin-top:8px;background:var(--ring);border:1px solid var(--ring);border-radius:8px;overflow:hidden}
.pi{flex:1;background:var(--surface-2);padding:6px 10px;cursor:help;min-width:0}
.pk{display:block;font-size:9px;letter-spacing:.09em;text-transform:uppercase;color:var(--muted);font-weight:700}
.pv2{display:block;font-size:11.5px;color:var(--ink-2);margin-top:2px;font-variant-numeric:tabular-nums}
.pv2 b{color:var(--ink)}
.pdd{display:block;font-size:10.5px;color:var(--muted);margin-top:1px;font-variant-numeric:tabular-nums}
.pdd.bon{color:var(--s-reel);font-weight:700}
.pdd.mal{color:var(--s-fric);font-weight:700}
.pmv{display:flex;gap:12px;flex-wrap:wrap;margin-top:7px;font-size:10.5px;color:var(--muted)}
.pmv b{color:var(--ink-2)}
.pnote{margin-top:6px;font-size:10.5px;color:var(--muted);line-height:1.45}
.pnote b{color:var(--ink-2)}
.pnote.combo{color:var(--s-traj)}
.pnote.combo b{color:var(--s-traj)}
.pnote.ko{color:var(--s-fric)}
.mini.undo{border-color:rgba(208,59,59,.5)}
.endturn.armed{background:var(--s-traj);color:#fff}

.gp.prev .chip{border-color:var(--s-traj);border-style:dashed}
.c2r b u{text-decoration:none;color:var(--s-traj);margin-left:4px;font-size:9px}
.c2p{margin-top:5px;font-size:9px;color:var(--s-traj);border-top:1px solid var(--grid);padding-top:4px}

.pfx{color:var(--s-traj);letter-spacing:0;text-transform:none;font-weight:700;margin-left:5px;cursor:help}
/* le cockpit regroupe sous la carte ce qui était éparpillé en haut de page :
   position, trajectoire, compteurs du monde et courbe d'évolution. */
.cockpit{margin-top:7px;border:1px solid var(--ring);border-radius:10px;background:var(--surface-2);padding:8px 11px}
.ckh{display:flex;align-items:flex-end;gap:13px;flex-wrap:wrap;padding-bottom:7px;
 border-bottom:1px solid var(--ring);margin-bottom:5px}
.ckm{cursor:help;min-width:92px}
.ckm .ct{font-size:9.5px;letter-spacing:.11em;text-transform:uppercase;color:var(--muted);font-weight:700;display:block;margin-bottom:1px}
.ckm .hv{font-size:21px}
.ckm .hs{font-size:9.5px;margin-top:0}
.cksc{flex:1 1 190px;max-width:300px;min-width:150px;cursor:help;padding-bottom:2px}
.ckk{display:flex;gap:13px;flex-wrap:wrap;align-items:flex-end;margin-left:auto;padding-bottom:2px}
.ckc{cursor:help;text-align:right;min-width:52px}
.ckc span{display:block;font-size:9px;color:var(--muted);text-transform:uppercase;letter-spacing:.07em;white-space:nowrap}
.ckc b{font-size:14.5px;font-variant-numeric:tabular-nums}
.ckc b small{font-size:9.5px;color:var(--muted);margin-left:1px}
.ckc.dmg b{font-size:12.5px}
.ckc.dmg i{font-style:normal;color:var(--s-base)}
.ckc.dmg u{text-decoration:none;color:var(--s-autres)}
.hv{font-size:26px;font-weight:700;letter-spacing:-.02em;line-height:1.05}
.hs{font-size:10.5px;color:var(--muted);margin-top:2px}
.kv{display:flex;justify-content:space-between;align-items:baseline;gap:10px;padding:2px 0;font-size:11.5px}
.kv .k{color:var(--ink-2)}
.kv .v{font-weight:700;font-variant-numeric:tabular-nums;white-space:nowrap}
.kv .v small{font-size:10px;color:var(--muted);margin-left:2px;font-weight:600}
.rl{display:flex;justify-content:space-between;font-size:11px;color:var(--ink-2);margin-top:4px}
.dtrack{height:7px;border-radius:4px;background:#2a2a28;overflow:hidden;margin-top:2px}
.dfill{height:100%;border-radius:4px;transition:width .4s}
.dfill.base{background:var(--s-base)}.dfill.autres{background:var(--s-autres)}

.board{display:grid;grid-template-columns:272px 1fr 272px;gap:1px;background:var(--ring)}
.mapcol{background:var(--surface);padding:9px 6px}
.mapbox{position:relative;width:100%}
svg.map{display:block;width:100%;height:auto;background:var(--sea);border-radius:10px;transition:background .9s}
/* halo d'émissions : il rétrécit quand le bloc décarbone. C'est l'objectif de la partie, dessiné. */
.halo{position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);border-radius:50%;pointer-events:none;z-index:0;
 background:radial-gradient(circle,rgba(208,59,59,var(--ho,.12)) 0%,rgba(208,59,59,calc(var(--ho,.12)*.4)) 52%,transparent 72%);
 transition:width .8s,height .8s,background .8s}
.gp.evt .chip{animation:evtp 2.4s ease-in-out infinite}
@keyframes evtp{0%,100%{box-shadow:0 0 0 0 rgba(201,133,0,0)}50%{box-shadow:0 0 0 5px rgba(201,133,0,.28)}}
.bl{fill:var(--bf,var(--land));stroke:#0d0d0d;stroke-width:2;transition:fill .9s}
.bl.on{fill:var(--land-hi)}
.bl.tgt{fill:#3d4a58}
.bl.lock{fill:var(--land-lock)}
.gp{position:absolute;transform:translate(-50%,-50%);cursor:pointer;z-index:2}
.chip{display:flex;align-items:flex-end;gap:3px;background:rgba(13,13,13,.86);border:1px solid var(--ring);
 border-radius:7px;padding:4px 5px;transition:transform .12s,border-color .12s}
.gp:hover .chip{transform:scale(1.1);border-color:var(--ink-2)}
.gp.hot .chip{border-color:rgba(208,59,59,.85)}
.gp.tgt .chip{border-color:var(--s-traj);box-shadow:0 0 0 2px rgba(57,135,229,.35)}
.gp.lock .chip{opacity:.55}
.cb{width:6px;height:24px;position:relative;display:flex;align-items:flex-end}
.cb span.bg{position:absolute;inset:0;background:#33332f;border-radius:2px}
.cb i{display:block;width:100%;border-radius:2px 2px 0 0;position:relative;transition:height .4s}
.cb.reel i{background:var(--s-reel)}
.cb.percu i{border:1.5px solid var(--s-percu);border-bottom:0;
 background:repeating-linear-gradient(135deg,rgba(201,133,0,.32) 0 3px,transparent 3px 6px)}
.cb.fric i{background:var(--s-fric)}
.cb.contr i{background:var(--s-traj)}
.chip b{font-size:8.5px;letter-spacing:.05em;color:var(--ink-2);margin-left:2px;align-self:center;font-weight:700}
.gp .card2{position:absolute;left:50%;top:50%;transform:translate(-50%,-50%) scale(.9);opacity:0;pointer-events:none;
 background:rgba(8,8,8,.97);border:1px solid var(--ring);border-radius:9px;padding:8px 10px;width:158px;
 transition:opacity .12s,transform .12s;box-shadow:0 8px 28px rgba(0,0,0,.7);z-index:3}
.gp:hover .card2{opacity:1;transform:translate(-50%,-50%) scale(1)}
.gp.hot .card2{border-color:rgba(208,59,59,.6)}
.c2h{display:flex;align-items:baseline;gap:6px;margin-bottom:5px;white-space:nowrap}
.c2h b{font-size:11px}.c2h span{font-size:9.5px;color:var(--muted);margin-left:auto}
.c2r{display:grid;grid-template-columns:46px 1fr 30px;align-items:center;gap:6px;font-size:9.5px;color:var(--muted);margin:2px 0}
.c2t{height:6px;border-radius:3px;background:#33332f;position:relative;overflow:hidden}
.c2f{position:absolute;inset:0 auto 0 0;border-radius:3px}
.c2f.reel{background:var(--s-reel)}
.c2f.percu{border:1.5px solid var(--s-percu);background:repeating-linear-gradient(135deg,rgba(201,133,0,.32) 0 3px,transparent 3px 6px)}
.c2f.fric{background:var(--s-fric)}
.c2f.contr{background:var(--s-traj)}
.c2r b{color:var(--ink-2);text-align:right;font-variant-numeric:tabular-nums;font-size:9.5px}
.c2a{margin-top:5px;font-size:9px;font-weight:700;color:var(--s-fric);border-top:1px solid var(--grid);padding-top:4px}
.c2c{margin-top:5px;font-size:9px;color:var(--muted);border-top:1px solid var(--grid);padding-top:4px}
.traj{margin-top:7px;border:1px solid var(--ring);border-radius:10px;background:var(--surface-2);padding:8px 11px}
.trh{display:flex;align-items:baseline;gap:12px;flex-wrap:wrap}
.trh h3{font-size:12px}
.trl{display:flex;gap:12px;margin-left:auto;font-size:10.5px;color:var(--ink-2)}
.trl i{display:inline-block;width:12px;border-top:2px solid;vertical-align:middle;margin-right:4px}
.traj svg{display:block;width:100%;height:auto;overflow:visible}
/* ---------- le camp adverse ----------
   Sans ce panneau, l'adversaire n'est qu'une baisse de jauge inexpliquée.
   Il faut qu'on voie sa main à l'œuvre : ce qu'il vient de jouer, où, contre quoi,
   et ce qu'il a laissé en place. */
.advp{border:1px solid rgba(208,59,59,.32);border-left:3px solid var(--s-fric);border-radius:8px;
 background:linear-gradient(180deg,rgba(208,59,59,.07),transparent 60%);padding:7px 9px;font-size:11px}
.advp .avx{color:var(--muted);font-size:10.5px;line-height:1.45}
.advl{display:grid;grid-template-columns:34px 1fr;gap:7px;align-items:baseline;padding:3px 0;cursor:help;
 border-bottom:1px solid rgba(255,255,255,.045)}
.advl:last-child{border-bottom:0}
.advl .ai{font-size:9px;font-weight:800;color:var(--s-fric);letter-spacing:.04em;font-variant-numeric:tabular-nums}
.advl .an{font-size:11px;color:var(--ink);line-height:1.25}
.advl .an i{display:block;font-style:normal;font-size:9.5px;color:var(--muted);margin-top:1px}
.advl .an u{text-decoration:none;color:var(--s-percu)}
.advs{margin-top:6px;padding-top:5px;border-top:1px solid rgba(255,255,255,.07);font-size:10px;color:var(--muted)}
.advs b{color:var(--ink-2)}
.advi{font-size:10px;color:var(--ink-2);padding:2px 0;cursor:help;line-height:1.35}
.advi b{font-weight:600}
.advi span{display:block;color:var(--muted)}
.advt{font-size:9.5px;letter-spacing:.09em;text-transform:uppercase;color:var(--muted);font-weight:700;margin:7px 0 2px}
/* marque des blocs travaillés par l'adversaire */
.chip{position:relative}
.gp.adv .chip{border-color:rgba(208,59,59,.85)}
.gp.adv0 .chip{animation:advp 1.9s ease-in-out infinite}
@keyframes advp{0%,100%{box-shadow:0 0 0 0 rgba(208,59,59,0)}50%{box-shadow:0 0 0 4px rgba(208,59,59,.22)}}
.advm{position:absolute;top:-7px;right:-7px;background:var(--s-fric);color:#140a0a;font-size:8.5px;font-weight:800;
 border-radius:8px;padding:0 4px;font-style:normal;line-height:15px;box-shadow:0 0 0 2px rgba(13,13,13,.92)}
.tag.rendu{border-color:rgba(25,158,112,.55);color:var(--s-reel)}
.tag.lourd{border-color:rgba(214,158,46,.6);color:#d69e2e;font-weight:700}
.c2adv{margin-top:5px;font-size:9px;color:var(--s-fric);border-top:1px solid var(--grid);padding-top:4px}
.badv{border-left:2px solid var(--s-fric);padding:4px 0 5px 8px;margin:5px 0;font-size:11px}
.badv span{color:var(--muted);font-size:10px;margin-left:6px}
.badv i{display:block;font-style:normal;color:var(--ink-2);font-size:10.5px;margin-top:2px}
.badv u{display:block;text-decoration:none;color:var(--s-fric);font-size:10px;margin-top:3px}

/* les deux commandes de la main au même endroit : renouveler, et voir ce qui n'est pas encore ouvert */
.hctl{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin:-4px 0 2px;
 border:1px solid var(--ring);border-radius:8px;padding:5px 7px;background:var(--surface-2);font-size:10.5px;color:var(--muted)}
.hctl .hcs{margin-right:auto;color:var(--ink-2);font-weight:600}
.hctl .lnk{font-size:10.5px}
.legend{display:flex;gap:13px;flex-wrap:wrap;padding:7px 6px 0;color:var(--muted);font-size:10.5px}
.legend i{display:inline-block;width:9px;height:9px;border-radius:2px;vertical-align:-1px;margin-right:5px}
/* ---------- colonne droite : main + journal ---------- */
.side{background:var(--surface);padding:10px;display:flex;flex-direction:column;gap:8px;min-width:0}
.side .ct{font-size:9.5px;letter-spacing:.11em;text-transform:uppercase;color:var(--muted);font-weight:700;
 display:flex;align-items:baseline;gap:8px}
.side .ct span{margin-left:auto;color:var(--ink-2);font-weight:600}
.hand{display:flex;flex-direction:column;gap:5px}
.card{border:1px solid var(--ring);border-radius:8px;background:var(--surface-2);padding:6px 8px;
 display:flex;flex-direction:column;gap:3px;cursor:pointer;position:relative;transition:border-color .12s,background .12s}
.card:hover{border-color:var(--ink-2)}
.card.sel{border-color:var(--s-traj);background:#1f2733;box-shadow:0 0 0 1px var(--s-traj)}
.card.no{opacity:.42;cursor:not-allowed}
.card .id{font-size:9.5px;letter-spacing:.09em;color:var(--muted);font-weight:700}
.card h4{font-size:11.5px;line-height:1.22;padding-right:14px}
.card .eff{font-size:10px;color:var(--ink-2);line-height:1.35;display:-webkit-box;-webkit-line-clamp:2;
 -webkit-box-orient:vertical;overflow:hidden}
.card:hover .eff{-webkit-line-clamp:6}
.meta{display:flex;gap:3px;flex-wrap:wrap}
.tag{font-size:9px;padding:1px 4px;border-radius:4px;border:1px solid var(--ring);color:var(--ink-2);white-space:nowrap}
.tag.cost{border-color:rgba(57,135,229,.45);color:#8fbdf2}
.tag.now{border-color:rgba(12,163,12,.5);color:#5bd15b}
.tag.slow{border-color:rgba(137,135,129,.55);color:var(--muted)}
.tag.trap{border-color:rgba(250,178,25,.55);color:#fab219}
.hint{border:1px dashed rgba(57,135,229,.6);border-radius:8px;padding:7px 9px;font-size:11px;color:#8fbdf2;
 background:rgba(57,135,229,.07)}
.jour{border:1px solid var(--ring);border-radius:9px;background:var(--surface-2);padding:8px 10px;
 font-size:11px;color:var(--ink-2);max-height:360px;overflow:auto;flex:1}
.jour .l{padding:2px 0;border-bottom:1px solid rgba(255,255,255,.04)}
.jour .l:last-child{border:0}
.jour .l b{color:var(--ink)}
.jour .l.g{color:#5bd15b}.jour .l.r{color:#e88}.jour .l.t{color:var(--muted);font-style:italic}
.jour .l.a{color:#7d7b75;font-size:10.5px}
.jour .l.a b{color:#a5a39b}
.jour .th{color:var(--muted);font-size:9.5px;letter-spacing:.09em;text-transform:uppercase;font-weight:700;
 margin-top:6px;padding-bottom:2px}
.jour .sh{color:var(--ink-2);font-size:9.5px;letter-spacing:.06em;text-transform:uppercase;font-weight:700;
 margin-top:7px;padding:3px 0 2px;border-top:1px solid var(--grid);opacity:.75}

/* ---------- le guide d'entrée ----------
   Le reproche des parties d'essai : on ne sait pas par où commencer. Un texte de
   règles ne le règle pas — il faut montrer les zones dans l'ordre où on s'en sert.
   Découpe dans un voile, une légende, huit étapes, sortie à tout moment. */
#guide{position:fixed;inset:0;z-index:60;display:none}
#guide.on{display:block}
#gtrou{position:fixed;border-radius:12px;pointer-events:none;border:1px solid rgba(255,255,255,.4);
 box-shadow:0 0 0 9999px rgba(5,5,5,.80);transition:left .3s,top .3s,width .3s,height .3s}
#gcard{position:fixed;width:340px;max-width:calc(100vw - 24px);background:var(--surface);
 border:1px solid var(--ring);border-radius:12px;padding:14px 16px;box-shadow:0 18px 50px rgba(0,0,0,.75);
 transition:left .3s,top .3s}
#gcard .gn{font-size:9.5px;letter-spacing:.11em;text-transform:uppercase;color:var(--muted);font-weight:700}
#gcard h4{font-size:15px;margin:3px 0 6px}
#gcard p{margin:0;font-size:12.5px;line-height:1.55;color:var(--ink-2)}
#gcard p b{color:var(--ink)}
.gb{display:flex;align-items:center;gap:8px;margin-top:12px}
.gb .lnk{margin-right:auto;font-size:11px}
.gb .mini.go{background:var(--ink);color:#0b0b0b;border-color:var(--ink);font-weight:700}
#tip{position:fixed;z-index:99;pointer-events:none;max-width:310px;background:#000;
 border:1px solid rgba(255,255,255,.18);border-radius:8px;padding:9px 11px;font-size:11.5px;line-height:1.45;
 color:var(--ink-2);opacity:0;transition:opacity .1s;box-shadow:0 6px 24px rgba(0,0,0,.6)}
#tip b{color:var(--ink)}
#tip .tl{display:flex;justify-content:space-between;gap:14px;font-variant-numeric:tabular-nums}
[data-tip]{cursor:help}

#ovl{position:fixed;inset:0;background:rgba(0,0,0,.7);display:none;z-index:50;align-items:center;justify-content:center;padding:22px}
#ovl.open{display:flex}
.modal{background:var(--surface);border:1px solid var(--ring);border-radius:14px;width:min(760px,100%);
 max-height:88vh;overflow:auto;box-shadow:0 20px 60px rgba(0,0,0,.75)}
.mh{display:flex;align-items:baseline;gap:12px;padding:15px 19px;border-bottom:1px solid var(--ring);
 background:var(--surface-2);position:sticky;top:0;z-index:2}
.mh h2{font-size:18px}.mh .sub{color:var(--muted);font-size:12px}
.mh .x{margin-left:auto;border:1px solid var(--ring);background:transparent;color:var(--ink-2);
 border-radius:7px;width:28px;height:28px;cursor:pointer;font-size:15px;line-height:1}
.mb{padding:15px 19px 19px;display:grid;grid-template-columns:1fr 1fr;gap:16px}
.mb .full{grid-column:1/-1}
.ms{font-size:9.5px;letter-spacing:.11em;text-transform:uppercase;color:var(--muted);font-weight:700;margin-bottom:7px}
.gauge{margin-bottom:11px}
.gh2{display:flex;justify-content:space-between;align-items:baseline;font-size:12px;color:var(--ink-2)}
.gh2 b{font-variant-numeric:tabular-nums;color:var(--ink)}
.gt{height:10px;border-radius:5px;background:#2a2a28;margin-top:4px;position:relative;overflow:hidden}
.gf{position:absolute;inset:0 auto 0 0;border-radius:5px}
.gf.reel{background:var(--s-reel)}
.gf.percu{border:2px solid var(--s-percu);background:repeating-linear-gradient(135deg,rgba(201,133,0,.32) 0 4px,transparent 4px 8px)}
.gf.fric{background:var(--s-fric)}
.gf.contr{background:var(--s-traj)}
.gx{font-size:10.5px;color:var(--muted);margin-top:3px}
.prof{display:grid;grid-template-columns:auto 1fr;gap:4px 12px;font-size:12px}
.prof dt{color:var(--muted)}.prof dd{margin:0;color:var(--ink-2)}
.mlist{margin:0;padding-left:16px;font-size:12px;color:var(--ink-2)}
.mlist li{margin:3px 0}
.mlist li.k{color:var(--s-traj)}
.mnote{font-size:12px;color:var(--ink-2);border-left:3px solid var(--s-percu);padding-left:10px}
.mnote.hot{border-left-color:var(--s-fric)}
/* ---------- fin de partie ---------- */
.fin .hv{font-size:52px}
.finrow{display:grid;grid-template-columns:1fr 1fr;gap:14px}
.fbox{border:1px solid var(--ring);border-radius:10px;background:var(--surface-2);padding:13px 15px}
.fbox .lab{font-size:9.5px;letter-spacing:.11em;text-transform:uppercase;color:var(--muted);font-weight:700}
.fbox .num{font-size:30px;font-weight:700;margin-top:4px;letter-spacing:-.02em}
.fbox .sub{font-size:11px;color:var(--muted);margin-top:3px}
.verdict{font-size:15px;line-height:1.55;color:var(--ink-2);border-left:3px solid var(--s-traj);padding-left:12px}
.verdict b{color:var(--ink)}
.again{border:0;border-radius:9px;padding:12px 20px;font-weight:700;background:var(--ink);color:#0b0b0b;cursor:pointer}
@media (max-width:1400px){.board{grid-template-columns:1fr 272px}.sideL{grid-column:1/-1;flex-direction:row;flex-wrap:wrap;max-height:none}.sideL>*{flex:1 1 240px}}
@media (max-width:1180px){.board{grid-template-columns:1fr}.sideL,.sideR{max-height:none}.mb{grid-template-columns:1fr}.opts{grid-template-columns:1fr}
  /* l'accueil ne fait que 820 px : ses deux cartes de camp tiennent côte à côte bien plus longtemps */
  .camps .opts{grid-template-columns:1fr 1fr}}
@media (max-width:640px){.camps .opts{grid-template-columns:1fr}}

/* ---------- doctrine ---------- */
.doc{background:var(--surface);border:1px solid var(--line);border-radius:8px;padding:10px 12px;margin-top:8px}
.doc.home{margin-top:0}
.drow{margin:9px 0 11px}
.drow:first-child{margin-top:2px}
.dlab{display:flex;justify-content:space-between;align-items:baseline;font-size:11.5px;margin-bottom:3px}
.dlab b{font-size:12px;color:var(--ink)}
.dlab .dv{color:var(--s-traj);font-weight:600;font-size:11px;letter-spacing:.02em}
.dends{display:flex;justify-content:space-between;font-size:10px;color:var(--muted);margin-top:1px}
input[type=range].dsl{-webkit-appearance:none;appearance:none;width:100%;height:4px;border-radius:3px;
  background:linear-gradient(90deg,var(--s-percu),var(--s-traj));outline:none;cursor:pointer;margin:0}
input[type=range].dsl::-webkit-slider-thumb{-webkit-appearance:none;width:14px;height:14px;border-radius:50%;
  background:var(--ink);border:2px solid var(--plane);cursor:pointer;box-shadow:0 0 0 1px var(--line)}
input[type=range].dsl::-moz-range-thumb{width:14px;height:14px;border-radius:50%;background:var(--ink);
  border:2px solid var(--plane);cursor:pointer}
input[type=range].dsl:disabled{opacity:.45;cursor:not-allowed}
.dfoot{font-size:10.5px;color:var(--muted);margin-top:8px;padding-top:7px;border-top:1px solid var(--line);line-height:1.45}
.dfoot b{color:var(--s-traj)}
/* ---------- fiche levier ---------- */
.card .info{position:absolute;top:5px;right:6px;width:16px;height:16px;border-radius:50%;
  border:1px solid var(--line);color:var(--muted);font-size:10px;line-height:14px;text-align:center;
  cursor:pointer;background:var(--surface-2);font-style:italic;font-family:Georgia,serif}
.card .info:hover{color:var(--ink);border-color:var(--ink)}
.card{position:relative}
.fl-h{display:flex;align-items:baseline;gap:8px;flex-wrap:wrap;margin-bottom:2px}
.fl-h h3{margin:0;font-size:16px}
.fl-fam{font-size:10.5px;color:var(--muted);border:1px solid var(--line);border-radius:3px;padding:1px 5px}
.fl-sec{margin-top:11px}
.fl-sec h5{margin:0 0 3px;font-size:10.5px;letter-spacing:.06em;text-transform:uppercase;color:var(--muted);font-weight:600}
.fl-sec p{margin:0;font-size:12.5px;line-height:1.5}
.fl-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(112px,1fr));gap:7px;margin-top:11px}
.fl-kv{background:var(--surface-2);border:1px solid var(--line);border-radius:5px;padding:6px 8px}
.fl-kv span{display:block;font-size:9.5px;color:var(--muted);text-transform:uppercase;letter-spacing:.05em}
.fl-kv b{font-size:11.5px;font-weight:500}
.fl-doc{margin-top:11px;background:var(--surface-2);border:1px solid var(--line);border-left:2px solid var(--s-traj);
  border-radius:5px;padding:8px 10px;font-size:11.5px;line-height:1.5}
.fl-doc em{color:var(--muted);font-style:normal}
.fl-blocs{display:flex;flex-wrap:wrap;gap:5px;margin-top:6px}
.fl-b{font-size:10.5px;border:1px solid var(--line);border-radius:3px;padding:2px 6px;background:var(--surface-2)}
.fl-b b{color:var(--s-traj)}


.dsum{display:grid;grid-template-columns:1fr 1fr;gap:6px 10px}
.dsum span{font-size:11px;color:var(--s-traj);display:block;line-height:1.3}
.dsum i{display:block;font-style:normal;font-size:9.5px;color:var(--muted);text-transform:uppercase;letter-spacing:.04em}

.sideL,.sideR{max-height:calc(100vh - 40px);overflow-y:auto;scrollbar-width:thin}
.sideL::-webkit-scrollbar,.sideR::-webkit-scrollbar{width:6px}
.sideL::-webkit-scrollbar-thumb,.sideR::-webkit-scrollbar-thumb{background:var(--ring);border-radius:3px}

/* ---------- animations et vie ---------- */
.c2f,.cb i{transition:width .45s cubic-bezier(.4,0,.2,1),height .45s cubic-bezier(.4,0,.2,1)}
@keyframes bump{0%{transform:scale(1)}35%{transform:scale(1.14)}100%{transform:scale(1)}}
@keyframes ring{0%{box-shadow:0 0 0 0 var(--fxc)}100%{box-shadow:0 0 0 16px transparent}}
.gp.fx .chip{animation:bump .55s ease}
.gp.fx{--fxc:rgba(122,168,255,.55)}
.gp.fx.bad{--fxc:rgba(208,59,59,.55)}
.gp.fx .chip{border-radius:7px;animation:bump .55s ease,ring .7s ease}
.flot{position:absolute;transform:translate(-50%,-50%);font-size:14px;font-weight:700;pointer-events:none;
  z-index:40;text-shadow:0 1px 6px #000;animation:up 1.15s ease forwards;white-space:nowrap}
@keyframes up{0%{opacity:0;margin-top:0}18%{opacity:1}100%{opacity:0;margin-top:-38px}}
.flot.up{color:var(--s-traj)}.flot.down{color:var(--s-fric)}.flot.pc{color:var(--s-percu)}
.flot.re{color:var(--s-reel)}.flot.ko{color:#e88}.flot.ok{color:#5bd15b}
/* bandeau de résolution */
#reso{position:absolute;inset:0;z-index:30;display:none;pointer-events:none}
#reso.on{display:block}
.rsHead{position:absolute;left:50%;top:14px;transform:translateX(-50%);background:rgba(12,12,12,.93);
  border:1px solid var(--ring);border-radius:9px;padding:9px 18px;font-size:14px;font-weight:600;
  letter-spacing:.02em;box-shadow:0 8px 28px rgba(0,0,0,.6);min-width:230px;text-align:center}
.rsHead small{display:block;font-size:10.5px;font-weight:500;color:var(--muted);letter-spacing:.06em;
  text-transform:uppercase;margin-bottom:3px}
.rsHead.g{border-color:rgba(91,209,91,.45)}.rsHead.r{border-color:rgba(208,59,59,.5)}
.rsHead.h{border-color:rgba(122,168,255,.45)}
.rsSkip{position:absolute;right:10px;top:14px;pointer-events:auto;font-size:11px;color:var(--muted);
  background:var(--surface-2);border:1px solid var(--ring);border-radius:6px;padding:5px 10px;cursor:pointer}
.rsSkip:hover{color:var(--ink)}
/* jet de dé */
.rsDe{position:absolute;left:50%;top:78px;transform:translateX(-50%);background:rgba(12,12,12,.95);
  border:1px solid var(--ring);border-radius:9px;padding:10px 14px;width:250px;box-shadow:0 8px 28px rgba(0,0,0,.6)}
.rsDe h5{margin:0 0 6px;font-size:11.5px;font-weight:600}
.deT{height:7px;border-radius:4px;background:var(--surface-2);position:relative;overflow:hidden;border:1px solid var(--ring)}
.deT i{position:absolute;left:0;top:0;bottom:0;background:linear-gradient(90deg,var(--s-reel),var(--s-traj));display:block}
.deT u{position:absolute;top:-3px;width:2px;height:13px;background:var(--ink);transition:left .5s ease}
.deR{margin-top:6px;font-size:12px;font-weight:700}
.deR.ok{color:#5bd15b}.deR.ko{color:#e88}
.deL{display:flex;justify-content:space-between;font-size:9.5px;color:var(--muted);margin-top:3px}
/* défi : le statut est une pastille, pas une phrase noyée */
.ev.defi .st{display:inline-block;font-size:9.5px;padding:1px 7px;border-radius:4px;
  border:1px solid rgba(201,133,0,.45);color:var(--s-percu);font-weight:700;
  text-transform:uppercase;letter-spacing:.05em}
.ev.defi.ok .st{color:var(--s-reel);border-color:rgba(25,158,112,.5)}
.ev.defi.warn .st{color:var(--s-fric);border-color:rgba(208,59,59,.5)}
.dfw{display:flex;align-items:center;gap:8px;margin-top:1px}
.dfw small{font-size:9.5px;color:var(--muted)}
/* presse */
.pr{border:1px solid var(--ring);border-radius:8px;background:var(--surface-2);padding:7px 10px;margin-bottom:6px}
.pr b{display:block;font-size:11.5px;line-height:1.35;font-weight:500}
.pr span{font-size:9.5px;color:var(--muted);text-transform:uppercase;letter-spacing:.06em}
/* score */
.scb{margin-top:7px}
.scb .sct{height:5px;border-radius:3px;background:var(--surface-2);border:1px solid var(--ring);overflow:hidden}
.scb .scf{height:100%;background:linear-gradient(90deg,var(--s-percu),var(--s-traj));transition:width .6s ease}
.scb .scl{display:flex;justify-content:space-between;font-size:10px;color:var(--muted);margin-top:3px}
.scb .scl b{color:var(--s-traj);font-weight:600}
/* combos */
.cmb{border:1px solid rgba(122,168,255,.45);border-radius:8px;background:rgba(122,168,255,.07);
  padding:7px 10px;margin-bottom:6px}
.cmb b{display:block;font-size:11.5px;color:var(--s-traj)}
.cmb span{font-size:10.5px;color:var(--ink-2);line-height:1.4;display:block;margin-top:2px}
.card .pb{position:absolute;bottom:6px;right:8px;font-size:10px;color:var(--s-percu);font-weight:600}

/* ---------- fiche levier lisible ---------- */
u.gl{text-decoration:underline dotted;text-underline-offset:2px;cursor:help;text-decoration-color:var(--muted)}
u.gl:hover{text-decoration-color:var(--ink)}
.fl-t{width:100%;border-collapse:collapse;font-size:11.5px;margin-top:2px}
.fl-t th{text-align:left;font-size:9.5px;text-transform:uppercase;letter-spacing:.05em;color:var(--muted);
  font-weight:600;padding:0 6px 4px 0;border-bottom:1px solid var(--line)}
.fl-t td{padding:4px 6px 4px 0;border-bottom:1px solid rgba(255,255,255,.04);vertical-align:top;line-height:1.4}
.fl-t td:first-child{white-space:nowrap;color:var(--ink)}
.fl-t td:last-child{text-align:right;white-space:nowrap}
.fl-s{display:block;font-style:normal;font-size:10.5px;color:var(--muted);margin-top:2px;white-space:normal}
.fl-t tr.ko{opacity:.38}
.fl-l{margin:4px 0 0;padding-left:16px;font-size:11.5px;line-height:1.55}
.fl-l li{margin-bottom:3px}
.fl-x{margin:6px 0 0;font-size:10.5px;color:var(--muted);line-height:1.45}
.fl-net{margin-top:8px;padding-top:7px;border-top:1px solid var(--line);font-size:11.5px;line-height:1.5}
.fl-net2{margin-top:5px;font-size:10.5px;color:var(--muted);line-height:1.45}
.fl-doc ul{margin:6px 0 0}
.modal{max-width:1080px}

.lnk{text-decoration:underline;cursor:pointer;color:var(--s-traj);font-weight:600}
.lnk.off{color:var(--muted);cursor:not-allowed;text-decoration:none}
.card .id{display:flex;align-items:center;gap:5px}

.fl-al{font-size:13px;font-style:italic;color:var(--ink)}
.fl-al span{display:block;font-style:normal;font-size:11px;color:var(--muted);line-height:1.5;margin-top:4px}

.mh .fl-h h2{font-size:19px;line-height:1.2}
.mh .sub{display:block;margin-top:3px}
.mh{padding-right:52px}

/* ---------- bilan de fin de tour ---------- */
#modal.wide{width:min(1120px,100%)}
.mb.bilan{grid-template-columns:minmax(0,1fr) minmax(0,1.25fr)}
.bpr{font-size:11.5px;color:var(--ink-2);font-style:italic;padding:4px 0;border-bottom:1px solid rgba(255,255,255,.05)}
.bpr span{display:block;font-style:normal;font-size:9.5px;color:var(--muted);margin-top:1px}
.brow{display:grid;grid-template-columns:1fr auto 12px auto 46px;align-items:baseline;gap:6px;
 font-size:12px;padding:4px 0;border-bottom:1px solid rgba(255,255,255,.05)}
.brow:last-child{border:0}
.bk{color:var(--ink-2)}
.bav{color:var(--muted);font-variant-numeric:tabular-nums}
.bfl{color:var(--muted);font-size:10px;text-align:center}
.bap{color:var(--ink);font-weight:700;font-variant-numeric:tabular-nums;white-space:nowrap}
.bap b{color:var(--s-traj);font-weight:700;font-size:11px;margin-left:3px}
.dl{font-size:10.5px;font-weight:700;font-variant-numeric:tabular-nums;text-align:right;display:inline-block;min-width:36px}
.dl.bon{color:var(--s-reel)}
.dl.mal{color:var(--s-fric)}
.dl.nul{color:#4a4a46}
.bthead,.btr{display:grid;grid-template-columns:30px minmax(64px,1fr) repeat(4,56px) 82px;align-items:baseline;gap:5px}
.bthead{font-size:9px;letter-spacing:.06em;text-transform:uppercase;color:var(--muted);font-weight:700;
 padding-bottom:4px;border-bottom:1px solid var(--grid)}
.bthead span{text-align:right}
.btr{font-size:11.5px;padding:5px 0;border-bottom:1px solid rgba(255,255,255,.05)}
.btr.calme{opacity:.45}
.bcode{font-weight:700;color:var(--ink-2);font-size:10px;letter-spacing:.04em}
.bnom{color:var(--ink-2);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.bg{text-align:right;font-variant-numeric:tabular-nums;color:var(--ink);white-space:nowrap}
.bg .dl{min-width:0;margin-left:3px}
.bg.contr{color:var(--s-traj)}.bg.reel{color:var(--s-reel)}
.bg.percu{color:var(--s-percu)}.bg.fric{color:var(--s-fric)}
.bet{font-size:9.5px;color:var(--muted);text-align:right;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.bnext{font-size:11.5px;color:var(--ink-2);border-left:2px solid var(--s-percu);padding:3px 0 3px 9px;margin-top:6px;line-height:1.45}
.bnext b{color:var(--ink)}
.bfoot{display:flex;align-items:center;gap:14px;border-top:1px solid var(--ring);padding-top:13px;margin-top:2px}
.bchk{display:flex;align-items:center;gap:7px;font-size:11.5px;color:var(--muted);cursor:pointer}
.bchk input{accent-color:var(--s-traj);width:14px;height:14px;cursor:pointer}
.bfoot .mini{margin-left:auto;padding:9px 22px;font-weight:700;background:var(--ink);color:#0b0b0b;border-color:transparent}
.mb.bilan .jour{max-height:32vh}
@media(max-width:820px){.mb.bilan{grid-template-columns:1fr}
 .bthead,.btr{grid-template-columns:26px minmax(0,1fr) repeat(4,48px);}
 .bet{display:none}}

/* ---------- l'agenda du tour ---------- */
.agenda{display:flex;flex-wrap:wrap;align-items:center;gap:6px;padding:8px 14px;
 background:var(--surface);border-bottom:1px solid var(--ring)}
.agt{font-size:9.5px;letter-spacing:.11em;text-transform:uppercase;color:var(--muted);font-weight:700;
 margin-right:4px;cursor:help;white-space:nowrap}
.ag{display:inline-flex;align-items:baseline;gap:6px;border:1px solid var(--ring);border-left-width:3px;
 border-radius:7px;padding:3px 9px;font-size:11px;color:var(--ink-2);background:var(--surface-2);cursor:help;
 max-width:100%}
.ag b{color:var(--ink);font-weight:600;white-space:nowrap}
.ag i{font-style:normal;color:var(--muted);font-size:10.5px}
.ag.g{border-left-color:var(--s-reel)}
.ag.r{border-left-color:var(--s-fric)}
.ag.h{border-left-color:var(--muted);opacity:.72}
.ag.adv{border-left-color:var(--s-base);border-style:dashed;border-left-style:solid}
.agenda.dansbilan{background:transparent;border:0;padding:8px 0 0;gap:5px}
@media(max-width:900px){.ag i{display:none}}

/* ---------- la carte du défi ---------- */
.ev.defi{border-left-color:var(--s-percu);background:linear-gradient(180deg,rgba(201,133,0,.07),transparent 60%)}
.ev.defi.ok{border-left-color:var(--s-reel);background:linear-gradient(180deg,rgba(25,158,112,.10),transparent 60%)}
.ev.defi.warn{border-left-color:var(--s-fric)}
.ev.defi .eh h4{color:var(--s-percu);font-size:12.5px}
.ev.defi.ok .eh h4{color:var(--s-reel)}
.dfp{margin-top:1px}
.dft{height:6px;border-radius:3px;background:var(--grid);overflow:hidden}
.dff{height:100%;border-radius:3px;transition:width .35s ease}
.dff.go{background:var(--s-percu)}
.dff.ok{background:var(--s-reel)}
.dff.warn{background:var(--s-fric)}
.dfl{display:flex;align-items:baseline;gap:8px;margin-top:4px;font-size:10.5px;color:var(--muted)}
.dfl .rec{margin-left:auto;color:var(--s-percu);font-weight:700;white-space:nowrap;cursor:help}
.ev.defi.ok .dfl .rec{color:var(--s-reel)}
.st.ok{color:var(--s-reel);font-weight:600}

/* ---------- carte de levier : périmètre et jauges ---------- */
.card .id{display:flex;align-items:center;gap:5px;flex-wrap:wrap;padding-right:16px}
.scope{margin-left:auto;font-size:8.5px;letter-spacing:.05em;font-weight:700;padding:1px 5px;border-radius:4px;
 border:1px solid;white-space:nowrap;cursor:help}
.scope.b{color:var(--s-traj);border-color:rgba(57,135,229,.5);background:rgba(57,135,229,.10)}
.scope.g{color:var(--s-base);border-color:rgba(144,133,233,.55);background:rgba(144,133,233,.12)}
.card.glob{border-left:3px solid rgba(144,133,233,.55)}
.card.blk{border-left:3px solid rgba(57,135,229,.42)}
.jauge{display:flex;align-items:center;gap:6px;cursor:help;margin-top:1px}
.jl{font-size:8.5px;letter-spacing:.06em;text-transform:uppercase;color:var(--muted);font-weight:700;flex:0 0 auto}
.jt{flex:1;height:4px;border-radius:2px;background:var(--grid);overflow:hidden;min-width:24px}
.jt i{display:block;height:100%;border-radius:2px}
.jt i.hi{background:var(--s-traj)}
.jt i.mid{background:#5b6f8e}
.jt i.lo{background:#4a4a46}
.jauge b{font-size:9px;color:var(--ink-2);font-variant-numeric:tabular-nums;flex:0 0 auto}
.tag.pari{border-color:rgba(201,133,0,.55);color:var(--s-percu)}
.tag.lat{border-color:rgba(144,133,233,.5);color:var(--s-base)}
.tag.defi{border-color:rgba(201,133,0,.6);color:var(--s-percu);background:rgba(201,133,0,.10);font-weight:700}
.tag.combo{border-color:rgba(122,168,255,.55);color:var(--s-traj)}

/* ---------- survol de la courbe de température ---------- */
.trg .tp rect{cursor:crosshair}
.trg .gl{stroke:var(--ink-2);stroke-width:1;stroke-dasharray:3 3;opacity:0;pointer-events:none}
.trg .pp{fill:var(--s-traj);stroke:var(--surface);stroke-width:2;opacity:0;pointer-events:none}
.trg .pg{fill:var(--muted);opacity:0;pointer-events:none}
.trg .tp:hover .gl{opacity:.55}
.trg .tp:hover .pp,.trg .tp:hover .pg{opacity:1}

/* ---------- carte de l'objectif ---------- */
.ev.obj{border-left-color:var(--s-traj)}
.ev.obj.ok{border-left-color:var(--s-reel)}
.objt{height:6px;border-radius:3px;background:var(--grid);overflow:hidden;margin-top:1px}
.objf{height:100%;border-radius:3px;background:var(--s-traj);transition:width .35s ease}
.objf.ok{background:var(--s-reel)}
.objl{display:flex;gap:6px;flex-wrap:wrap;margin-top:4px}
.jal{font-size:9.5px;padding:1px 6px;border-radius:4px;border:1px solid var(--ring);
 color:var(--muted);white-space:nowrap;cursor:help;text-transform:none;letter-spacing:0;font-weight:400}
.jal.ok{border-color:rgba(25,158,112,.5);color:var(--s-reel);font-weight:700;background:rgba(25,158,112,.10)}

/* ---------- catalogue à venir ---------- */
.avl{display:grid;grid-template-columns:36px minmax(0,1fr) minmax(0,1.1fr);gap:10px;align-items:baseline;
 padding:6px 0;border-bottom:1px solid rgba(255,255,255,.05);cursor:pointer}
.avl:hover{background:rgba(255,255,255,.03)}
.avid{font-size:10px;font-weight:700;color:var(--muted);letter-spacing:.04em}
.avn{font-size:12.5px;color:var(--ink)}
.avn i{display:block;font-style:normal;font-size:10px;color:var(--muted);margin-top:1px}
.avr{font-size:11px;color:var(--s-percu);line-height:1.35}
</style>